import { PiNumberCircleSix } from "react-icons/pi";

type Props = {
  className?: string;
};

export default function Six({ className }: Props) {
  return <PiNumberCircleSix className={className} />;
}
