import { PiNumberCircleSeven } from "react-icons/pi";

type Props = {
  className?: string;
};

export default function Seven({ className }: Props) {
  return <PiNumberCircleSeven className={className} />;
}
